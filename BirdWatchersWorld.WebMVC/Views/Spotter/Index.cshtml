@model IEnumerable<BirdWatchersWorld.Models.Spotting.SpotterListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FullName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FullName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }, new { @style = "color:#ffae00;" }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }, new { @style = "color:#08c928;" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id }, new { @style = "color:#ff0000;" })
        </td>
    </tr>
}

</table>
